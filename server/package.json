{
  "name": "tokiserver-admin-backend",
  "version": "1.0.0",
  "description": "Backend de administración para Tokiserver (Entorno LAN)",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch --watch-exclude=logs --watch-exclude=users.json --watch-exclude=sessions.json index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.0"
  }
}
